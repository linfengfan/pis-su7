1.梳理联调需要接口
  const router = `{
        // 运营积分看板（与 managed 同级，独立 Tab 导航）
        path: 'ops',
        name: 'TeamOps',
        component: () => import('@/views/team/ops/index.vue'),
        meta: { title: '团队运营看板' },
        redirect: { name: 'TeamOpsMembers' },
        children: [
          {
            // 成员看板
            path: 'members',
            name: 'TeamOpsMembers',
            component: () => import('@/views/team/ops/pages/MembersPage.vue'),
            meta: { title: '成员看板' }
          },
          {
            // 项目看板
            path: 'projects',
            name: 'TeamOpsProjects',
            component: () => import('@/views/team/ops/pages/ProjectsPage.vue'),
            meta: { title: '项目看板' }
          },
          {
            // 排行榜
            path: 'ranking',
            name: 'TeamOpsRanking',
            component: () => import('@/views/team/ops/pages/RankingPage.vue'),
            meta: { title: '排行榜' }
          }
        ]
      },
      {
        // 项目消耗明细：项目看板「消耗明细」下钻页，项目 ID 由 query.id 传入。
        // 与 TeamOps 平级而非其子路由 —— 本页自带页头与视图分段，不套看板头部/Tab。
        path: 'ops/projects/consume',
        name: 'TeamOpsProjectConsume',
        component: () => import('@/views/team/ops/project-consume/index.vue'),
        meta: { title: '项目消耗明细' }
      },`
  const files = `${router}所设计到的所有文件，包含各页面、组件、工具函数等`
  const problem = `目前整个模块，src/router/modules/team.ts文件中=>${router}，设计到本次需求的范围=>${files}
  已完成页面ui和交互的开发，需要进入联调阶段，联调的页面为=>${router}。需要你完成：
    1.【按当前实现写到文档中 =>`docs/团队/需求集合/9-10|明谦/开发实现摘要.md`】
    2.【梳理出联调需要的接口名称、参数、返回值，并写到文档中 =>`docs/团队/需求集合/9-10|明谦/前端需要联调接口.md`】
  `
  使用方式await开启子Agent({
    agentType: "前端架构师",
    additionalToolAllowList: [
      // ========== Git 操作 ==========
      "Bash(git add *)",
      "Bash(git branch *)",
      "Bash(git checkout *)",
      "Bash(git commit -m *)",
      "Bash(git diff *)",
      "Bash(git restore *)",
      "Bash(git reset *)",
      "Bash(git show *)",
      "Bash(git status *)",

      // ========== 代码检查 ==========
      "Bash(pnpm type-check *)",
      "Bash(pnpm lint *)",
      "Bash(npx vue-tsc *)",
      "Bash(npx eslint *)",

      // ========== 文件读取（核心） ==========
      "Read(docs/**)",
      "Read(src/**)",
      "Read(.claude/**)",
      "Read(.cursor/**)",
      "Read(public/**)",
      "Read(node_modules/**/*.d.ts)",
      "Read(//tmp/**)",

      // ========== 配置文件 ==========
      "Read(package.json)",
      "Read(tsconfig.json)",
      "Read(vite.config.*)",
      "Read(tailwind.config.*)",

      // ========== MCP 工具 ==========
      "figma",          // Figma 设计稿读取
      "WebFetch",       // 网页内容抓取
      "WebSearch",      // 搜索

      // ========== 可选工具 ==========
      "Skill(code-review)",    // 代码审核
      "Bash(grep -E *)",      // 代码搜索
      "Bash(find . -name *)", // 文件查找
    ],
    prompt: `@.claude/agents/前端架构师.md\n\n解决下：${problem}`
  })
  子agent开启后不要关闭，常驻等候新任务下发，继续工作